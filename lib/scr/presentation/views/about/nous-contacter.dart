import 'package:flutter/material.dart';

class NousContacter extends StatelessWidget {
  const NousContacter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}